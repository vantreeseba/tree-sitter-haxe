package my.test.package;

@:stupid
class Main<T, U> {
	public var foo:Int;
	public var bar:Void;
	public var foo:Array<Int>;

	public static function main(a:Int, a, f) {
		var foo = x++;
		var b = 3 + 3;
		x++;
		++x;
	}
}

typedef Bar = {
	var x:Int;
}

typedef Bar = Main;
