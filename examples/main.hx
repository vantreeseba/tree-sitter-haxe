package my.test.package;

@:some_macro
class Main<T, U> {
	public var foo:Int;
	public var bar:Void;
	public var foo:Array<Int>;

	public static function main(a:Int, a, f) {
		var foo = x++;
		var b = 3 + a;
		x++;
		++x;

		return foo;
	}
}

typedef Bar = {
	var x:Int;
}

typedef Bar = Main;
