package my.test.package;

typedef Point = {
	var x:Int;
	var y:Int;
}

@:some_macro
@:some_macro(test.bar)
class Main<T, U> {
	public var foo:Int;
	public var bar:Void;
	public var foo:Array<Int>;

	public static function main(a:Int, a, f) {
		var foo = x++;
		var b = 3 + a;
		x++;
		++x;

		return foo;
	}
}

var x = [1, 2, 3];
var x = {x: 1};
var x = {x: (1 + 3)};
var x = [x => (1 + 3)];
typedef Bar = Main;
var foo = new Main();
